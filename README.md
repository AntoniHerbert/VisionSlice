# Instruções para rodar o projeto: #

- Instalar o docker;

- Clonar o repositório;

- Rodar o comando <code>docker compose -f docker-compose.yml up --build -d</code>;

- Acessar a porta localhost:8000;
